[package]
name = "legwork"
version = "0.1.0"
description = "Legwork mission escrow, worker reputation and SKR staking"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "legwork"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
anchor-spl = "1.0.2"
